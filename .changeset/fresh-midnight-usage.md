---
'@rivus/agent-presence': patch
---

Refresh all built-in usage sources at the first session boundary after midnight so inactive sources contribute zero instead of leaving the signature stuck at `今日 —`.
