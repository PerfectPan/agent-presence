# Changelog

## Unreleased

- Add the initial `@rivus/agent-presence` CLI for syncing coding-agent lifecycle events to Feishu signature link previews.
- Support Codex, Claude Code, opencode hooks, JSON state, TTL expiration, debounce, macOS power reset, and the `feishu-signature` provider backed by `l.garyyang.work`.
- Add npm package metadata, validation commands, CI, and tag-based publishing workflow.
